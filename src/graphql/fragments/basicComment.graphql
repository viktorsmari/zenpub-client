fragment BasicComment on Comment {
  localId
  content
  id
  published
  author {
      name
      id
      icon
      preferredUsername
      localId
  }
  replies {
      totalCount
  }
  inReplyTo {
    localId
    content
    author {
      id
      icon
      name
      localId
      preferredUsername
    }
  }
  likers {
    totalCount
  }
  context {
    __typename
    ... on Community {
      id
      name
      localId
    }
      ... on Collection {
      id
      name
      localId
    }
  }
}