fragment BasicCollection on Collection {
    id
    canonicalUrl
    preferredUsername
    name
    summary
    icon
    isLocal
    isPublic
    myFollow {
        id
    }
    community {
        id
        canonicalUrl
        isLocal
        isPublic
        name
        icon
        myFollow { 
            id
        }
    }
    followers { totalCount }
    threads { totalCount }
    outbox { totalCount }
}