input ResourceInput {
    name: String!
    summary: String
    url: String!
    license: String
    primaryLanguageId: String
    icon: String
}

 mutation updateResourceMutation($resourceId: Int!, $resource: ResourceInput!) {
    updateResource(resourceId: $resourceId, resource: $resource) {
        id
        name
        summary
        url
        license
        icon
        isLocal
        isPublic
        isHidden
        createdAt
        updatedAt
        primaryLanguage { id englishName localName }
    }
}

