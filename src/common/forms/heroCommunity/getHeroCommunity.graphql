
query getHeroCommunity($communityId: String!) {
    community(communityId: $communityId) {
        id
        preferredUsername
        name
        summary
        icon
        image
        followers {
            totalCount
        }
        myFollow { id }
        creator { id }
    }
}
