import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { PropsTable } from '@storybook/components';
import {editCollectionContext} from 'ui/mock/context'
import { EditCollectionModal, EditCollectionContext } from 'ui/modules/EditCollectionModal';
import { action } from '@storybook/addon-actions';
import {Box} from 'rebass'

<Meta title="Modules|EditCollectionModal" component={EditCollectionModal}/>

# EditCollectionModal
Let's define a story for the `Activity preview` module

<Preview>
  <Story name="edit collection module">
    <EditCollectionContext.Provider value={editCollectionContext}>
       <Box height={680}>
       <EditCollectionModal 
         collectionId={'1'}
         closeModal={action('close modal')}
        />
        </Box>
    </EditCollectionContext.Provider>
  </Story>
</Preview>

