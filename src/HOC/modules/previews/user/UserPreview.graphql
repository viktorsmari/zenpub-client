fragment UserPreview on User {
  icon
  image
  userId: id 
  userName: name
  displayUsername
  summary
  canonicalUrl
  # isLocal
  myFollow { id }
  # likerCount
  # myLike { id }
  myFlag { id }
}