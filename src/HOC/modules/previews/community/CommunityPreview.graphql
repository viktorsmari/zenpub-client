fragment CommunityPreview on Community {
    ...CommunityInfo
    id
    icon
    summary
    myFlag { id }
    collectionCount
    followerCount
    threads { totalCount }
    # myLike { id }
    # likerCount
}

fragment CommunityInfo on Community {
    id
    name
    myFollow { id }
}
