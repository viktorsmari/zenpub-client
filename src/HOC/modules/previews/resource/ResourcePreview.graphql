fragment ResourcePreview on Resource {
    id
    # isLocal
    icon
    name
    summary
    canonicalUrl
    url
    myLike { id }
    myFlag { id }
    likers { totalCount }
    collection {
      id
      community {
        id
        ...CommunityInfo
      }
    }
    isLocal
    license
    # likerCount
}