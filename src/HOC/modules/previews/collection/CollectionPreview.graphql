fragment CollectionPreview on Collection {
    id
    icon{ id, url }
    isLocal
    canonicalUrl
    name
    summary
    resourceCount
    displayUsername
    myFlag { id }
    myFollow { id }
    # myLike { id }
    # likerCount
    community {
        id
        ...CommunityInfo
    }
}
