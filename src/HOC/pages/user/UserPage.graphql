

fragment UserPageActivities on Activity {
  id
}

fragment UserPageUserData on User {
  id
  userId:id
  collectionFollows{
    totalCount
  }
  communityFollows{
    totalCount
  }
  userFollows{
    totalCount
  }
  outbox{
    totalCount
  }
  website
  userName:name
  
  ...HeroUserUserData
}
